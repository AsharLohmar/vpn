ensure_proxy=0
d_args+=( -p "${PROXY_ENDPOINT}:${PROXY_PORT}:8443" -p "${PROXY_ENDPOINT}:${PROXYPAC_PORT}:8088" --env "PROXY_ENDPOINT=${PROXY_ENDPOINT}:${PROXY_PORT}" )
d_args+=( -d asharlohmar/glider-proxy )

